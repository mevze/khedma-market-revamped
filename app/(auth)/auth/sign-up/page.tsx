import { db } from "@/lib/db";
import AuthLayout from "../_components/auth-layout";

const SignUpPage = () => {
  return (
    <AuthLayout>
      sign up
    </AuthLayout>
  );
};

export default SignUpPage;
