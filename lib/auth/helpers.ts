import "server-only";


/**
 * this function is used to get the current logged in user
 * @returns the current logged in user
 * @example
 * ```ts
 * const { isAuthenticated, user } = await auth();
 *  ```
 */
const auth  = async () => {
    // TODO: implement the logic to get the current logged in user
    
}



export { auth };